<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice - <%= invoice.number %></title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');

        :root {
            --gold-color: #a1824f; /* A sophisticated, slightly muted gold */
            --gold-accent-light: #f3efe8;
            --bg-color: #fdfaf6; /* Light brownish background */
            --bg-accent-color: #fbf7f2; /* Slightly darker accent for summary */
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            font-size: 14px;
            color: #333;
            background-color: var(--bg-color);
            margin: 0;
            padding: 20px;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }

        .invoice-wrapper {
            max-width: 850px;
            margin: 0 auto;
            background: #fff;
            border: 1px solid #e5e7eb;
            border-top: 5px solid var(--gold-color);
            border-radius: 8px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05), 0 2px 4px -1px rgba(0, 0, 0, 0.04);
        }

        .invoice-header {
            padding: 20px 24px 16px 24px;
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            border-bottom: 1px solid #e5e7eb;
        }

        .company-logo {
            max-width: 150px;
            height: auto;
        }

        .invoice-title-section {
            text-align: right;
        }

        .invoice-title {
            font-size: 36px;
            font-weight: 700;
            color: var(--gold-color);
            margin: 0;
        }

        .invoice-meta {
            margin-top: 8px;
            font-size: 14px;
            color: #6b7280;
        }

        .invoice-meta .label {
            font-weight: 600;
            color: #374151;
        }

        .invoice-parties {
            padding: 18px 24px 10px 24px;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 18px;
        }

        .party-details h3 {
            font-size: 12px;
            font-weight: 600;
            color: var(--gold-color);
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin: 0 0 12px 0;
            border-bottom: 1px solid var(--gold-accent-light);
            padding-bottom: 6px;
        }

        .party-details p {
            margin: 0;
            line-height: 1.7;
            color: #374151;
        }

        .party-name {
            font-weight: 600;
            color: #111827;
        }

        .invoice-body {
            padding: 0 24px 18px 24px;
        }

        .items-table {
            width: 100%;
            border-collapse: collapse;
        }

        .items-table thead {
            border-bottom: 2px solid #d1d5db;
        }

        .items-table th {
            text-align: left;
            padding: 12px 8px;
            font-size: 12px;
            font-weight: 600;
            color: #6b7280;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .items-table td {
            padding: 16px 8px;
            vertical-align: top;
            border-bottom: 1px solid #e5e7eb;
        }

        .items-table tbody tr:last-child td {
            border-bottom: none;
        }

        .item-description .item-name {
            font-weight: 600;
            color: #111827;
            margin-bottom: 4px;
        }

        .item-description .item-meta {
            font-size: 12px;
            color: #6b7280;
        }

        .th-qty, .td-qty { text-align: center; }
        .th-price, .td-price { text-align: right; }
        .th-total, .td-total { text-align: right; }

        .invoice-summary {
            padding: 14px 24px 14px 24px;
            background-color: #fff;
            border-top: 2px solid #e5e7eb;
            display: flex;
            justify-content: flex-end;
        }

        .summary-table {
            width: 100%;
            max-width: 320px;
        }

        .summary-table td {
            padding: 8px 0;
            color: #374151;
        }

        .summary-table .summary-label {
            font-weight: 500;
        }

        .summary-table .summary-value {
            text-align: right;
            font-weight: 500;
        }

        .summary-table .summary-total {
            border-top: 2px solid #d1d5db;
            padding-top: 12px;
        }

        .summary-table .summary-total .summary-label,
        .summary-table .summary-total .summary-value {
            font-size: 20px;
            font-weight: 700;
            color: var(--gold-color);
        }

        .invoice-footer {
            padding: 14px 24px;
            border-top: 1px solid #e5e7eb;
            text-align: center;
            font-size: 12px;
            color: #6b7280;
        }

        .footer-thanks {
            font-size: 16px;
            font-weight: 500;
            color: #111827;
            margin-bottom: 12px;
        }
    </style>
</head>
<body>
    <div class="invoice-wrapper">
        <header class="invoice-header">
            <div class="company-details">
                <img src="<%= company.logoUrl %>" alt="<%= company.name %>" class="company-logo">
            </div>
            <div class="invoice-title-section">
                <h1 class="invoice-title">INVOICE</h1>
                <p class="invoice-meta">
                    <span class="label">Invoice #:</span> <%= invoice.number %><br>
                    <span class="label">Date:</span> <%= formatDate(invoice.date) %>
                </p>
            </div>
        </header>

        <section class="invoice-parties">
            <div class="party-details">
                <h3>From</h3>
                <p>
                    <strong class="party-name"><%= company.name %></strong><br>
                    <%= company.address %><br>
                    <%= company.city %>, <%= company.state %> <%= company.postalCode %><br>
                    <%= company.country %><br>
                    <%= company.email %>
                </p>
            </div>
            <div class="party-details">
                <h3>Bill To</h3>
                <p>
                    <strong class="party-name"><%= customer.name %></strong><br>
                    <%= customer.address.street %><br>
                    <%= customer.address.city %>, <%= customer.address.state %> <%= customer.address.postalCode %><br>
                    <%= customer.address.country %><br>
                    <%= customer.email %>
                </p>
            </div>
        </section>

        <main class="invoice-body">
            <table class="items-table">
                <thead>
                    <tr>
                        <th style="width: 50%;">Description</th>
                        <th class="th-qty" style="width: 10%;">Qty</th>
                        <th class="th-price" style="width: 20%;">Unit Price</th>
                        <th class="th-total" style="width: 20%;">Total</th>
                    </tr>
                </thead>
                <tbody>
                    <% order.items.forEach(function(item) { %>
                    <tr>
                        <td>
                            <div class="item-description">
                                <div class="item-name"><%= item.displayName || (item.product ? item.product.name : 'N/A') %></div>
                                <div class="item-meta">
                                    <% if (item.variant && item.variant.name) { %>
                                        Variant: <%= item.variant.name %><br>
                                    <% } %>
                                    <% if (item.displayMetals && item.displayMetals.length > 0) { %>
                                        Materials: <% item.displayMetals.forEach(function(metal, idx) { %><%= metal.type %> <%= metal.purity %> (<%= metal.weight %>g)<%= idx < item.displayMetals.length - 1 ? ', ' : '' %><% }); %>
                                    <% } %>
                                </div>
                            </div>
                        </td>
                        <td class="td-qty"><%= item.quantity %></td>
                        <td class="td-price"><%= formatCurrency(item.price) %></td>
                        <td class="td-total"><strong><%= formatCurrency(item.itemTotal) %></strong></td>
                    </tr>
                    <% }); %>
                </tbody>
            </table>
        </main>

        <section class="invoice-summary">
            <table class="summary-table">
                <tr>
                    <td class="summary-label">Subtotal</td>
                    <td class="summary-value"><%= formatCurrency(order.subtotal) %></td>
                </tr>
                <tr>
                    <td class="summary-label">Tax (3%)</td>
                    <td class="summary-value"><%= formatCurrency(order.tax) %></td>
                </tr>
                <tr>
                    <td class="summary-label">Shipping</td>
                    <td class="summary-value"><%= formatCurrency(order.shippingFee) %></td>
                </tr>
                <tr class="summary-total">
                    <td class="summary-label">Total</td>
                    <td class="summary-value"><%= formatCurrency(order.totalPrice) %></td>
                </tr>
            </table>
        </section>

        <footer class="invoice-footer">
            <p class="footer-thanks">Thank you for your business!</p>
            <p><%= company.name %> | <%= company.email %> | <%= company.website %></p>
            <p style="margin-top: 16px; font-size: 10px;">Generated on <%= generatedDate %></p>
        </footer>
    </div>
</body>
</html>
