<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice - <%= invoice.number %></title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            line-height: 1.6;
            color: #333;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
        }

        .invoice-container {
            max-width: 800px;
            margin: 0 auto;
            background: #fff;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            overflow: hidden;
        }

        /* Header with elegant gold gradient */
        .invoice-header {
            background: linear-gradient(135deg, #d4af37 0%, #ffd700 50%, #b8860b 100%);
            color: #fff;
            padding: 30px;
            text-align: center;
            position: relative;
        }

        .invoice-header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="jewelry" patternUnits="userSpaceOnUse" width="20" height="20"><circle cx="10" cy="10" r="2" fill="rgba(255,255,255,0.1)"/></pattern></defs><rect width="100" height="100" fill="url(%23jewelry)"/></svg>');
            opacity: 0.3;
        }

        .logo-section {
            position: relative;
            z-index: 1;
        }

        .company-logo {
            max-width: 120px;
            height: auto;
            margin-bottom: 15px;
        }

        .company-name {
            font-size: 28px;
            font-weight: bold;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }

        .company-tagline {
            font-size: 14px;
            opacity: 0.9;
        }

        /* Main content */
        .invoice-content {
            padding: 30px;
        }

        .invoice-details {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            margin-bottom: 30px;
        }

        .detail-section h3 {
            color: #d4af37;
            font-size: 16px;
            margin-bottom: 15px;
            border-bottom: 2px solid #f0f0f0;
            padding-bottom: 5px;
        }

        .detail-item {
            margin-bottom: 8px;
            display: flex;
            justify-content: space-between;
        }

        .detail-label {
            font-weight: 600;
            color: #666;
        }

        .detail-value {
            color: #333;
        }

        /* Items table */
        .items-section {
            margin: 30px 0;
        }

        .items-title {
            color: #d4af37;
            font-size: 20px;
            margin-bottom: 20px;
            text-align: center;
            border-bottom: 3px solid #f0f0f0;
            padding-bottom: 10px;
        }

        .items-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
        }

        .items-table th {
            background: linear-gradient(135deg, #d4af37 0%, #ffd700 100%);
            color: #fff;
            padding: 15px 12px;
            text-align: left;
            font-weight: 600;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .items-table td {
            padding: 15px 12px;
            border-bottom: 1px solid #eee;
            vertical-align: top;
        }

        .items-table tbody tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        .items-table tbody tr:hover {
            background-color: #f5f5f5;
        }

        .item-image {
            width: 50px;
            height: 50px;
            object-fit: cover;
            border-radius: 4px;
            border: 2px solid #ddd;
        }

        .item-name {
            font-weight: 600;
            color: #333;
            margin-bottom: 5px;
        }

        .item-variant {
            font-size: 12px;
            color: #666;
            font-style: italic;
        }

        .item-metals {
            font-size: 11px;
            color: #888;
            margin-top: 3px;
        }

        .text-right {
            text-align: right;
        }

        .text-center {
            text-align: center;
        }

        /* Summary section */
        .summary-section {
            margin-top: 30px;
            border-top: 3px solid #f0f0f0;
            padding-top: 20px;
        }

        .summary-table {
            width: 100%;
            max-width: 400px;
            margin-left: auto;
        }

        .summary-table td {
            padding: 8px 0;
            border-bottom: 1px solid #eee;
        }

        .summary-table .total-row {
            font-size: 18px;
            font-weight: bold;
            color: #d4af37;
            border-top: 2px solid #d4af37;
            border-bottom: 3px double #d4af37;
        }

        .summary-table .total-row td {
            padding: 15px 0;
        }

        /* Footer */
        .invoice-footer {
            background: #f8f9fa;
            padding: 25px 30px;
            text-align: center;
            border-top: 1px solid #eee;
            color: #666;
            font-size: 12px;
        }

        .footer-note {
            margin-bottom: 15px;
            font-style: italic;
        }

        .company-contact {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-top: 15px;
        }

        /* Status badges */
        .status-badge {
            display: inline-block;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .status-completed {
            background: #d4edda;
            color: #155724;
        }

        .status-pending {
            background: #fff3cd;
            color: #856404;
        }

        .status-processing {
            background: #cce7ff;
            color: #004085;
        }

        /* Responsive design */
        @media (max-width: 600px) {
            .invoice-details {
                grid-template-columns: 1fr;
                gap: 20px;
            }

            .items-table th,
            .items-table td {
                padding: 10px 8px;
                font-size: 12px;
            }

            .company-name {
                font-size: 24px;
            }

            .items-title {
                font-size: 18px;
            }
        }

        /* Print styles */
        @media print {
            body {
                background: #fff;
            }

            .invoice-container {
                box-shadow: none;
                border-radius: 0;
                margin: 0;
            }

            .invoice-footer {
                page-break-inside: avoid;
            }
        }

        /* Page break handling for multi-page invoices */
        .page-break {
            page-break-before: always;
        }

        .no-break {
            page-break-inside: avoid;
        }
    </style>
</head>
<body>
    <div class="invoice-container">
        <!-- Header -->
        <div class="invoice-header">
            <div class="logo-section">
                <img src="<%= company.logoUrl %>" alt="<%= company.name %>" class="company-logo">
                <h1 class="company-name"><%= company.name %></h1>
                <p class="company-tagline">Exquisite Handcrafted Jewelry</p>
            </div>
        </div>

        <!-- Main Content -->
        <div class="invoice-content">
            <!-- Invoice and Customer Details -->
            <div class="invoice-details">
                <!-- Invoice Information -->
                <div class="detail-section">
                    <h3>📋 Invoice Details</h3>
                    <div class="detail-item">
                        <span class="detail-label">Invoice Number:</span>
                        <span class="detail-value"><strong><%= invoice.number %></strong></span>
                    </div>
                    <div class="detail-item">
                        <span class="detail-label">Invoice Date:</span>
                        <span class="detail-value"><%= formatDate(invoice.date) %></span>
                    </div>
                    <div class="detail-item">
                        <span class="detail-label">Order ID:</span>
                        <span class="detail-value"><%= order.orderId %></span>
                    </div>
                    <div class="detail-item">
                        <span class="detail-label">Order Date:</span>
                        <span class="detail-value"><%= order.formattedDate %></span>
                    </div>
                    <div class="detail-item">
                        <span class="detail-label">Payment Status:</span>
                        <span class="status-badge status-<%= order.payment.paymentStatus %>">
                            <%= order.payment.paymentStatus %>
                        </span>
                    </div>
                </div>

                <!-- Customer Information -->
                <div class="detail-section">
                    <h3>👤 Customer Details</h3>
                    <div class="detail-item">
                        <span class="detail-label">Name:</span>
                        <span class="detail-value"><%= customer.name %></span>
                    </div>
                    <div class="detail-item">
                        <span class="detail-label">Email:</span>
                        <span class="detail-value"><%= customer.email %></span>
                    </div>
                    <div class="detail-item">
                        <span class="detail-label">Address:</span>
                        <span class="detail-value">
                            <%= customer.address.name %><br>
                            <%= customer.address.street %><br>
                            <%= customer.address.city %>, <%= customer.address.state %><br>
                            <%= customer.address.postalCode %>, <%= customer.address.country %>
                            <% if (customer.address.phone) { %>
                                <br>Phone: <%= customer.address.phone %>
                            <% } %>
                        </span>
                    </div>
                </div>
            </div>

            <!-- Order Items -->
            <div class="items-section no-break">
                <h2 class="items-title">💎 Order Items</h2>
                
                <table class="items-table">
                    <thead>
                        <tr>
                            <th style="width: 60px;">Image</th>
                            <th style="width: 40%;">Item Details</th>
                            <th style="width: 10%; text-align: center;">Qty</th>
                            <th style="width: 15%; text-align: right;">Unit Price</th>
                            <th style="width: 15%; text-align: right;">Total</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% order.items.forEach(function(item, index) { %>
                        <tr>
                            <td class="text-center">
                                <% if (item.primaryImage) { %>
                                    <img src="<%= item.primaryImage.image_url %>" alt="<%= item.product ? item.product.name : 'Product' %>" class="item-image">
                                <% } else { %>
                                    <div class="item-image" style="background: #f0f0f0; display: flex; align-items: center; justify-content: center; color: #999; font-size: 10px;">
                                        No Image
                                    </div>
                                <% } %>
                            </td>
                            <td>
                                <div class="item-name"><%= item.displayName || (item.product ? item.product.name : 'Product Name Not Available') %></div>
                                <% if (item.variant && item.variant.name) { %>
                                    <div class="item-variant" style="font-size: 12px; color: #666; margin-top: 2px;">
                                        Variant: <%= item.variant.name %>
                                    </div>
                                <% } %>
                                <% if (item.displayMetals && item.displayMetals.length > 0) { %>
                                    <div class="item-metals" style="font-size: 11px; color: #888; margin-top: 4px;">
                                        <strong>Materials:</strong>
                                        <% item.displayMetals.forEach(function(metal, idx) { %>
                                            <%= metal.type %> <%= metal.purity %> (<%= metal.weight %>g)<%= idx < item.displayMetals.length - 1 ? ', ' : '' %>
                                        <% }); %>
                                    </div>
                                <% } else if (item.product && item.product.metals && item.product.metals.length > 0) { %>
                                    <div class="item-metals" style="font-size: 11px; color: #888; margin-top: 4px;">
                                        <strong>Materials:</strong>
                                        <% item.product.metals.forEach(function(metal, idx) { %>
                                            <%= metal.type %> <%= metal.purity %> (<%= metal.weight %>g)<%= idx < item.product.metals.length - 1 ? ', ' : '' %>
                                        <% }); %>
                                    </div>
                                <% } %>
                            </td>
                            <td class="text-center">
                                <%= item.quantity %>
                            </td>
                            <td class="text-right">
                                <%= formatCurrency(item.price) %>
                            </td>
                            <td class="text-right">
                                <strong><%= formatCurrency(item.itemTotal) %></strong>
                            </td>
                        </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>

            <!-- Order Summary -->
            <div class="summary-section no-break">
                <table class="summary-table">
                    <tr>
                        <td><strong>Subtotal:</strong></td>
                        <td class="text-right"><%= formatCurrency(order.subtotal) %></td>
                    </tr>
                    <tr>
                        <td><strong>Tax (3%):</strong></td>
                        <td class="text-right"><%= formatCurrency(order.tax) %></td>
                    </tr>
                    <tr>
                        <td><strong>Shipping:</strong></td>
                        <td class="text-right"><%= formatCurrency(order.shippingFee) %></td>
                    </tr>
                    <tr class="total-row">
                        <td><strong>TOTAL AMOUNT:</strong></td>
                        <td class="text-right"><strong><%= formatCurrency(order.totalPrice) %></strong></td>
                    </tr>
                </table>
            </div>
        </div>

        <!-- Footer -->
        <div class="invoice-footer">
            <div class="footer-note">
                Thank you for choosing <%= company.name %>! We appreciate your business and trust in our exquisite jewelry craftsmanship.
            </div>
            
            <div class="company-contact">
                <div>
                    <strong>Address:</strong><br>
                    <%= company.address %><br>
                    <%= company.city %>, <%= company.state %> <%= company.postalCode %><br>
                    <%= company.country %>
                </div>
                <div>
                    <strong>Contact:</strong><br>
                    Phone: <%= company.phone %><br>
                    Email: <%= company.email %><br>
                    Web: <%= company.website %>
                </div>
            </div>
            
            <div style="margin-top: 20px; font-size: 11px; color: #999;">
                This is a computer-generated invoice. Generated on <%= generatedDate %>
            </div>
        </div>
    </div>
</body>
</html>